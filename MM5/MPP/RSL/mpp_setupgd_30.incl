CSTART   mpp_setupgd_30.incl
        IF ( RSL_IAMMONITOR() ) THEN
        READ (40)NP,JULGMTP,JYEAR,JMONTH,JDAY,JHOUR,DMSCR,IPSCR
        ENDIF
        CALL RSL_MON_BCAST(NP,IWORDSIZE)
        CALL RSL_MON_BCAST(JULGMTP,IWORDSIZE)
        CALL RSL_MON_BCAST(JYEAR,IWORDSIZE)
        CALL RSL_MON_BCAST(JMONTH,IWORDSIZE)
        CALL RSL_MON_BCAST(JDAY,IWORDSIZE)
        CALL RSL_MON_BCAST(JHOUR,IWORDSIZE)

CEND   mpp_setupgd_30.incl
